{
  "mcpServers": {
    "filesystem": {
      "command": "mcp-server-filesystem",
      "args": ["/home/user/meme"],
      "description": "Scoped file system access for the meme project"
    },
    "memory": {
      "command": "mcp-server-memory",
      "description": "Persistent key-value memory across sessions"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Structured step-by-step reasoning for complex problems"
    },
    "puppeteer": {
      "command": "mcp-server-puppeteer",
      "description": "Browser automation and screenshot testing for the meme UI"
    }
  }
}
